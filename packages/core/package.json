{
  "name": "@drizzle-sst/core",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "sst bind 'vitest'",
    "typecheck": "tsc -noEmit",
    "migrate": "drizzle-kit generate:pg --schema=./src/sql/schema.ts --out=migrations"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@types/pg": "^8.6.6",
    "drizzle-kit": "^0.20.14",
    "vitest": "^0.27.1"
  },
  "dependencies": {
    "@aws-sdk/client-rds-data": "^3.507.0",
    "drizzle-orm": "^0.29.3",
    "sst": "^2.40.3"
  }
}
